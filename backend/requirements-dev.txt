-r requirements.txt

# Test runner
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0

# In-memory database for tests — no Postgres required
aiosqlite==0.20.0

# Lint / format / types
ruff==0.8.6
mypy==1.14.1
